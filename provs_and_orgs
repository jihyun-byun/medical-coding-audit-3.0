SELECT
  DISTINCT(doctor_id),
  doctor_name,
  group_id,
  group_name
FROM
  note_data.providers_and_orgs
WHERE
  doctor_name NOT LIKE '%Duplicate%'
  OR doctor_name NOT LIKE '%Inactive%'
ORDER BY
  group_name